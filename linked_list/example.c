#include<stdio.h>
#include<stdlib.h>

int main()
{
    printf("\n double = %lu", sizeof(double));
    printf("\n pointer = %lu", sizeof(int*));
    printf("\n array subscript = %d", sizeof(unsigned int));
    return 0;
}
