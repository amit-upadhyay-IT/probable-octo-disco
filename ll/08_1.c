#include<stdio.h>
#include<stdlib.h>

struct node
{
    int data;
    struct node* random_link;
    struct node* next;
};

void insert_at_beginning(struct node* head, int data, struct node* r_link)
{

}
